# =================================================
# BACKEND PRODUCTION ENVIRONMENT VARIABLES
# =================================================
#
# SECURITY WARNING:
# - NEVER commit this file to git!
# - Add .env to .gitignore
# - Restrict file permissions: chmod 600 .env
# - Rotate secrets regularly
#
# Copy this file to: backend/.env
# Fill in the values with your actual credentials
# =================================================

# ─────────────────────────────────────────────────
# SERVER CONFIGURATION
# ─────────────────────────────────────────────────

NODE_ENV=production
PORT=3005

# Application URLs
FRONTEND_URL=https://onelastai.co
NEXT_PUBLIC_API_URL=https://onelastai.co
NEXT_PUBLIC_APP_URL=https://onelastai.co
NEXT_PUBLIC_BASE_URL=https://onelastai.co

# ─────────────────────────────────────────────────
# SECURITY & AUTHENTICATION
# ─────────────────────────────────────────────────

# JWT Configuration (CRITICAL: Use strong random strings!)
JWT_SECRET=CHANGE_THIS_TO_SUPER_SECURE_RANDOM_STRING_64_CHARS_MIN
NEXTAUTH_SECRET=CHANGE_THIS_TO_SUPER_SECURE_RANDOM_STRING_64_CHARS_MIN
NEXTAUTH_URL=https://onelastai.co

# Generate secrets with: openssl rand -base64 64

# ─────────────────────────────────────────────────
# DATABASE
# ─────────────────────────────────────────────────

# MongoDB Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/shiny-friend-disco?retryWrites=true&w=majority

# ─────────────────────────────────────────────────
# AI MODEL API KEYS
# ─────────────────────────────────────────────────

# OpenAI (GPT-4, GPT-3.5)
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY

# Anthropic (Claude 3.5 Sonnet, Claude 3 Opus)
ANTHROPIC_API_KEY=sk-ant-YOUR_ANTHROPIC_API_KEY

# Google Gemini (Gemini 1.5 Pro)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Cohere (Command R+)
COHERE_API_KEY=YOUR_COHERE_API_KEY

# ─────────────────────────────────────────────────
# TRANSLATION SERVICES
# ─────────────────────────────────────────────────

GOOGLE_TRANSLATE_API_KEY=YOUR_GOOGLE_TRANSLATE_API_KEY
GOOGLE_CLOUD_API_KEY=YOUR_GOOGLE_CLOUD_API_KEY
DEEPL_API_KEY=YOUR_DEEPL_API_KEY

# ─────────────────────────────────────────────────
# VOICE & SPEECH SERVICES
# ─────────────────────────────────────────────────

# ElevenLabs (Text-to-Speech)
ELEVENLABS_API_KEY=YOUR_ELEVENLABS_API_KEY

# Azure Speech Services
AZURE_SPEECH_KEY=YOUR_AZURE_SPEECH_KEY
AZURE_SPEECH_REGION=YOUR_AZURE_REGION

# ─────────────────────────────────────────────────
# EMAIL SERVICE (Choose One)
# ─────────────────────────────────────────────────

# SendGrid (Recommended)
SENDGRID_API_KEY=SG.YOUR_SENDGRID_API_KEY
SENDGRID_FROM_EMAIL=noreply@onelastai.co

# OR Resend (Alternative)
# RESEND_API_KEY=re_YOUR_RESEND_API_KEY
# RESEND_FROM_EMAIL=noreply@onelastai.co

# OR SMTP (Generic)
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USER=your-email@gmail.com
# MAIL_PASS=your-app-password
# MAIL_FROM=noreply@onelastai.co

# ─────────────────────────────────────────────────
# PAYMENT GATEWAYS
# ─────────────────────────────────────────────────

# Stripe (LIVE Keys!)
STRIPE_SECRET_KEY=sk_live_YOUR_STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_STRIPE_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_STRIPE_WEBHOOK_SECRET

# PayPal (LIVE Keys!)
PAYPAL_CLIENT_ID=YOUR_PAYPAL_LIVE_CLIENT_ID
PAYPAL_CLIENT_SECRET=YOUR_PAYPAL_LIVE_CLIENT_SECRET
PAYPAL_MODE=live

# ─────────────────────────────────────────────────
# AWS S3 STORAGE
# ─────────────────────────────────────────────────

AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_REGION=us-east-1
AWS_S3_BUCKET=shiny-friend-disco-assets

# ─────────────────────────────────────────────────
# RATE LIMITING & CACHING
# ─────────────────────────────────────────────────

# Redis (Recommended for production)
# Install: sudo apt install redis-server
# REDIS_URL=redis://localhost:6379

# If not using Redis, rate limiting uses in-memory store
# (Not recommended for multi-server deployments)

# ─────────────────────────────────────────────────
# CORS CONFIGURATION
# ─────────────────────────────────────────────────

ALLOWED_ORIGINS=https://onelastai.co,https://www.onelastai.co

# ─────────────────────────────────────────────────
# LOGGING & MONITORING
# ─────────────────────────────────────────────────

LOG_LEVEL=info
ENABLE_REQUEST_LOGGING=true

# Sentry (Optional - for error tracking)
# SENTRY_DSN=https://YOUR_SENTRY_DSN@sentry.io/PROJECT_ID

# ─────────────────────────────────────────────────
# ADDITIONAL SERVICES (Optional)
# ─────────────────────────────────────────────────

# IP Geolocation
IPINFO_TOKEN=YOUR_IPINFO_TOKEN

# Twilio (SMS/Voice - if needed)
# TWILIO_ACCOUNT_SID=YOUR_TWILIO_ACCOUNT_SID
# TWILIO_AUTH_TOKEN=YOUR_TWILIO_AUTH_TOKEN
# TWILIO_PHONE_NUMBER=+1234567890

# ─────────────────────────────────────────────────
# DEVELOPMENT/TESTING FLAGS (Set to false)
# ─────────────────────────────────────────────────

ENABLE_DEBUG=false
ENABLE_TEST_MODE=false
SKIP_EMAIL_VERIFICATION=false

# =================================================
# END OF BACKEND ENVIRONMENT VARIABLES
# =================================================
#
# Security Checklist:
# ✅ Changed all default secrets
# ✅ Used strong random strings (64+ chars)
# ✅ Set file permissions: chmod 600 .env
# ✅ Added .env to .gitignore
# ✅ Using HTTPS only (no HTTP)
# ✅ Rate limiting enabled
# ✅ CORS properly configured
# ✅ All API keys rotated and secure
#
# =================================================
