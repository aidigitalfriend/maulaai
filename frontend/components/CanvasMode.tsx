'use client';

// Canvas Mode - Now using proper component architecture
// Each component is separate and reusable

import { CanvasWorkspace } from './canvas';

export default function CanvasMode() {
  return <CanvasWorkspace />;
}
